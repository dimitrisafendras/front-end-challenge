import React from 'react';
import { AppLayout } from '../components/layouts/appLayout';
import { AppsList } from '../views';

export const App = () => (
  <AppLayout>
    <AppsList />
  </AppLayout>
);
